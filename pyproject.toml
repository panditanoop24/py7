
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py7"
version = "0.1.0"
description = "Minimal Python project"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
